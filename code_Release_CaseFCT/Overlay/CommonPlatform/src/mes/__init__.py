import os
from datetime import datetime

# mtime = max(os.stat(root).st_mtime for root, _, _ in os.walk(os.path.dirname(__file__)))
#
# version = 'V:1.0.1 (' + datetime.strftime(datetime.fromtimestamp(mtime), '%Y-%m-%d %H:%M:%S') + ')'
# print("version =", version)
